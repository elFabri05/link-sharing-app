# React + TypeScript + Express + Vite

training link sharing app

// *** netlify files to deploy and configs , but not being able to deploy due to a typescript incompatibility error in netlify environment.
